# 更新记录

## 0.16

- 新增 Windows 单文件 EXE，固定名称 LennyTorrentDownloader.exe，内嵌 Logo 和版本信息。
- 新增 Build.cmd 和固定依赖版本的构建脚本，Start.cmd 优先启动 EXE。
- 新增 EXE 独立诊断入口，验证下载引擎、Logo、托盘及视频解码。
- GitHub 备份只保留源码、资源和自动化测试代码；大型程序包独立放在 Release。

## 0.15

- 修复播放窗口关闭后仍保留失效对象引用，导致删除任务或退出时报错。
- 关闭播放器时立即注销播放会话、释放分片内存缓存和画面，忽略晚到的分片读取结果。
- 隔离新旧播放会话，避免旧窗口的读取请求在重新播放时继续生效。
- 新增真实部分下载视频播放后关闭窗口、清理缓冲、删除文件的完整回归。
- 正常退出仍保留未完成下载以供续传；删除文件需明确勾选对应选项。

## 0.14

- 任务名称及名称表头左对齐；进度、速度、连接数和状态统一居中，进度数字与进度条对齐。
- 新增设置持久化、选择文件、无效种子、双击打开、删除确认默认值和取消、完整传输内容校验、队列切换测试。
- 增加 RunTests.cmd 统一回归入口，独立运行各套件并输出测试报告。

## 0.13

- 在创建窗口前设置独立 Windows AppUserModelID，避免任务栏按 Python 解释器分组及显示 Python 图标。
- 续传数据显式保存种子元数据；停止的磁力任务重新载入时保留已知文件信息，修复删除失败。
- 尚未取得元数据、未产生下载内容的磁力任务直接移除，不向不存在的磁盘存储请求删除。
- 保留真正的文件删除错误，不再展示“删除失败：操作成功完成”的矛盾提示。
- 新增停止/重启后的磁力删除、无元数据磁力删除、无关文件保留及原生 Windows 应用标识和图标回归测试。

## 0.12

- 使用用户提供的 Logo，统一界面、窗口和托盘图标。
- 支持最小化到系统托盘，保持下载及状态保存运行。
- 托盘菜单支持恢复窗口、退出并停止下载；保留窗口关闭即退出的行为。
- 托盘提示显示当前任务数和传输速度，无托盘环境回退为普通最小化。

## 0.11

- 正式名称 Lenny's Torrent Downloader。
- 统一源码目录、独立运行环境及 Start.cmd 启动入口。
- 新增磁力链接导入和元数据持久化。
- 新增实验性边下边看：起播分片优先下载、本地 Range 服务、内置播放器。
- 区分暂停与停止；删除任务可选永久清理对应文件。
- 保留原任务数据兼容路径。
- 通过真实引擎和生成视频的集成测试。

早期原型 V1/V2 已归档，不再作为后续发布命名。
